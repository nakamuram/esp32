// 認証情報テンプレート
// このファイルを secrets.h にコピーして実際の値を記入する
//   cp include/secrets.h.example include/secrets.h
// secrets.h は .gitignore で除外されているためコミットされない
#pragma once

// Wi-Fi
#define WIFI_SSID     "your-ssid"
#define WIFI_PASSWORD "your-password"

// AWS（#8 で API Gateway + Lambda に決定）
// AWS_API_ENDPOINT は `terraform output api_endpoint`（infra/）の値をそのまま使う
// AWS_DEVICE_SECRET は infra/terraform.tfvars の device_secret と同じ値にする
#define AWS_API_ENDPOINT  "https://xxxxxxxxxx.execute-api.ap-northeast-1.amazonaws.com/readings"
#define AWS_DEVICE_SECRET "change-me-to-a-random-string"
#define DEVICE_ID         "esp32-sensor-01"
